# 商城后台管理系统测试环境配置

# 基础URL配置
base_url: "http://dev-mall-admin.example.com"
api_prefix: "/api/v1"

# 数据库配置
database:
  host: "localhost"
  port: 3306
  user: "root"
  password: "root123"
  database: "mall_admin_dev"
  charset: "utf8mb4"

# Redis配置
redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

# 测试账号
test_account:
  admin:
    username: "admin"
    password: "admin123"
  user:
    username: "testuser"
    password: "test123"

# 超时配置
timeout:
  request: 10
  page_load: 30

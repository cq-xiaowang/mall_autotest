# 商城后台管理系统生产环境配置(仅用于验证，谨慎使用)

# 基础URL配置
base_url: "http://prod-mall-admin.example.com"
api_prefix: "/api/v1"

# 数据库配置
database:
  host: "prod-db.example.com"
  port: 3306
  user: "prod_user"
  password: "prod_pass"
  database: "mall_admin_prod"
  charset: "utf8mb4"

# Redis配置
redis:
  host: "prod-redis.example.com"
  port: 6379
  password: ""
  db: 0

# 测试账号
test_account:
  admin:
    username: "admin"
    password: "admin123"
  user:
    username: "testuser"
    password: "test123"

# 超时配置
timeout:
  request: 10
  page_load: 30
